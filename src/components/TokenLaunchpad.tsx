import React from "react";

type Props = {};

const TokenLaunchpad = (props: Props) => {
  return (
    <div className="h-screen flex justify-center  items-center flex-col">
      TokenLaunchpad
    </div>
  );
};
export default TokenLaunchpad;
